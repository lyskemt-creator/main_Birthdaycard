abstract class Letter {
  char ch;
  float x, y;
  PFont font;
  color col;

  Letter(char ch, float x, float y, PFont font, color col) {
    this.ch = ch;
    this.x = x;
    this.y = y;
    this.font = font;
    this.col = col;
  }

  abstract void update();
  abstract void display();
}
